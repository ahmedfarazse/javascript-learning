
// Array in JavaScript

// In array we store multiply type of data in one array. 
// Start from index 0. 
// when we copy array it create shallow copy(take reference of the array). 

const arr1 = [1,2,3,4,5];
const names = ["Ahmed", "Faraz"];

const arr2 = new Array(1,2,3)


console.log(arr1[0]); 
console.log(arr2);
console.log(names[1]);
